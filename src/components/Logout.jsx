import React from "react";

const Logout = () => {
  const handleSubmit = () => {
    // Your code here
  };

  return (
    <form>
      <button type="button" onClick={handleSubmit}>
        Disconnect
      </button>
    </form>
  );
};

export default Logout;
